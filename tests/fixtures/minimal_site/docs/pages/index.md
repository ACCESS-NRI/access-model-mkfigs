# Minimal Fixture Site

This is a tiny stand-in doc tree used only to prove that `mkdocs build`
succeeds end-to-end against the same theme/plugin combination the real
paper repos use, without depending on the full real repo, its custom
theme overrides, or its GitHub-hosted third-party plugins.

<!-- experiments -->
